@model IEnumerable<view_eye.Models.Test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doctors_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patients_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnswersId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Doctors_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patients_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AnswersId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
